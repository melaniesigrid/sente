// en · legal
/* The small print. The documents themselves live in src/content/legal.js,
   where a test reads them; these are the few words the screen says on its own. */
export const legal = {
  eyebrow: "The small print",
  stamp: "Last changed {date}",
  tabs: "Small print",
  /* Shown on every language but the one the documents were written in. A
     translated contract is a courtesy, not a second contract: saying which
     version governs is the honest half of offering one at all. */
  translated: "This is a translation, offered so it can be read. The English version is the one that governs.",
};

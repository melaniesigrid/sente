/* ----------------------- LESSON CONTENT ------------------------
   Lessons live one per file under lessons/<tier>/ and are indexed by
   library.js. `LESSONS` stays exported here as an alias of the whole
   library so older imports keep working. */
export { LIBRARY as LESSONS } from "./library.js";
